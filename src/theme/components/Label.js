import variables from "./../variables/platform";

export default (variables = variables) => {
  const labelTheme = {
    ".focused": {
      width: 0
    },
    fontSize: 17
  };

  return labelTheme;
};
